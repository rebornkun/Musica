import './Slider.css'

import { Boxlist } from '../Boxlist';

const Slider = ({List}) => {
    return ( 
        <div className="slider">
            {List}
        </div>
     );
}
 
export default Slider;